# ===========================================================
#  RAILWAY ENVIRONMENT VARIABLES — Production Reference
#  ⚠️  DO NOT commit secrets to Git.
#  Set these one by one in Railway → Service → Variables tab
# ===========================================================

# ─── Server ──────────────────────────────────────────────────
NODE_ENV=production
PORT=5000

# ─── Database ─────────────────────────────────────────────────
# Use the INTERNAL Railway URL (faster, free, private network)
DATABASE_URL=postgresql://postgres:ULRrpsbJqxANbOYxlWtwbjyQtDvEyYQd@postgres.railway.internal:5432/railway

# ─── Security ─────────────────────────────────────────────────
SESSION_SECRET=hosp_alarm_session_secret_xyz789

# ─── ESP32 Controller Auth ────────────────────────────────────
# Must EXACTLY match CLOUD_DEVICE_KEY in esp32/controller/config.h
DEVICE_API_KEY=super

# ─── MQTT (leave blank → embedded Aedes broker runs on PORT) ──
# MQTT_BROKER_HOST=
# MQTT_BROKER_PORT=1883
# MQTT_BROKER_USER=
# MQTT_BROKER_PASS=

# ─── CORS / Frontend ─────────────────────────────────────────
# Comma-separated list of allowed frontend origins
# FRONTEND_URL=https://your-vercel-app.vercel.app
